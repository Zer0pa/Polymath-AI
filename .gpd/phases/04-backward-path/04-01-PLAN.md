# Plan 04-01: Rank-4 Adapter Backward

## Goal

Implement the smallest real phone-side gradient path: a rank-4 post-layer0
residual adapter trained against a fixed layer1 target, with RunPod PyTorch as
oracle only.

## Tasks

1. Generate deterministic adapter fixture and PyTorch gradient reference on
   RunPod.
2. Run adapter backward kernels on REDMAGIC OpenCL.
3. Compare phone gradients against PyTorch with cosine threshold `0.99`.

## Result

Passed in
`runtime/reports/gemma4_megakernel/backward_path/20260517T040000Z_g5_rank4_adapter_opencl/gate_result.json`.
