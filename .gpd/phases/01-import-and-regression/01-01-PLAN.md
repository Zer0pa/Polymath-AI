---
phase: "01"
plan: "01-01"
title: "Import Gemma4 Kernel and preserve G1 regression harness"
status: "ready"
objective: "Import policy-approved Gemma4 Kernel source/evidence into Polymath and verify the host build plus G1 replay path without forbidden artifacts."
---

# Plan 01-01: Import Gemma4 Kernel And Preserve G1 Regression Harness

## Gate

G2 passes only if Polymath can build the imported host code and can point to the
G1 phone gate without artifact leakage. It does not pass the terminal training
objective.

## Tasks

1. Import only approved upstream files into
   `integrations/gemma4-snapdragon-megakernel/`:
   - G1 evidence docs;
   - `gemma4_megakernel` source, headers, CMake, android scripts, comparator,
     and reference generator;
   - `executor_ir` schema and tiny-block train-step example;
   - `model_spec/gemma4_e2b.json` and `model_spec/gemma4_e4b.json`;
   - `polymath_native` source subset excluding build outputs and caches;
   - `runtime/overnight/20260516T032712Z/native_kernel_gate.json` as text
     evidence.
2. Update `MANIFEST.md` with upstream commit hashes, imported paths, commands,
   and policy exclusions.
3. Add a G1 regression runbook under the integration namespace.
4. Run an artifact leak scan for forbidden extensions and directories.
5. Run Mac host CMake build and CTest for imported `gemma4_megakernel`.
6. Record report artifacts under
   `runtime/reports/gemma4_megakernel/import_and_regression/<run_id>/`.
7. Run a falsifier checklist for G2 before promotion.

## Verification

- No imported file matches forbidden classes.
- Mac CMake/CTest passes or exact blocker is recorded.
- G1 phone/RunPod replay commands are present and still point to the passed
  authority gate evidence.
- G1 is not inflated into training.
