# Plan 05-01: Phone-Side Adapter SGD

## Goal

Perform a real phone-side optimizer update for the declared rank-4 adapter
while preserving frozen Gemma base hashes.

## Tasks

1. Run the same adapter backward path on REDMAGIC OpenCL.
2. Apply SGD update on phone to `adapter_a` and `adapter_b`.
3. Compare updated adapter tensors against RunPod reference.
4. Verify frozen layer0/layer1 safetensors hashes remain unchanged.

## Result

Passed in
`runtime/reports/gemma4_megakernel/optimizer_update/20260517T040000Z_g6_rank4_adapter_sgd/gate_result.json`.
