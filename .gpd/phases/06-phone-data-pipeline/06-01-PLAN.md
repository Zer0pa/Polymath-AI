# Plan 06-01: Phone HF Stream Tokenize Pack

## Goal

Build the first phone-native data path stage: Hugging Face raw text fetched by
the phone, native Gemma BPE tokenization on phone CPU, and UFS packed sequence
cache.

## Tasks

1. Export Gemma tokenizer BPE tables from `tokenizer.json`.
2. Fetch a license-clean CC0 Hugging Face text slice on the phone with system
   `curl`.
3. Tokenize and pack on the phone using the native runner.
4. Compare token IDs and attention masks against RunPod `transformers`.

## Result

Passed in
`runtime/reports/gemma4_megakernel/phone_data_pipeline/20260517T040000Z_g7_hf_native_token_pack/gate_result.json`.
