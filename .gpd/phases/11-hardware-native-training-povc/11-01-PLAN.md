---
phase: 11-hardware-native-training-povc
plan: "01"
type: execute
wave: 1
depends_on: ["10-02"]
files_modified:
  - docs/PRD-HARDWARE-NATIVE-TRAINING-POVC.md
  - integrations/gemma4-snapdragon-megakernel/gemma4_megakernel/src/runner/main.cpp
  - integrations/gemma4-snapdragon-megakernel/gemma4_megakernel/src/backends/opencl_layer_runner.cpp
  - integrations/gemma4-snapdragon-megakernel/gemma4_megakernel/include/polymath/gemma4/adapter_training.h
  - scripts/host/
  - runtime/reports/gemma4_megakernel/hardware_native_povc/
interactive: false
conventions:
  runtime_topology: "phone is authority; Mac starts/inspects/pulls; RunPod builds and provides references"
  tensor_layout: "batch-major sequence tensors unless manifest states otherwise"
  authority_metric: "phone-native queue-run training evidence with non-regressing G1/G3/relevant G8"
  artifact_policy: "no weights, raw tensors, SDK binaries, tokens, env files, .venv, node_modules, or build caches in git"
contract:
  scope:
    question: "Can the Phase 10 narrow lane become a phone-resident, hardware-native, capability-moving Gemma4 training POVC without host per-iteration orchestration?"
  claims:
    - id: claim-h11-daemon
      statement: "A long-lived phone-side runner can execute queued training experiments without ADB per iteration and can resume from phone-local state."
    - id: claim-h11-bottleneck
      statement: "The 36.7 s/iteration Phase 10 dead time is decomposed into measured sources."
    - id: claim-h11-scope-objective
      statement: "A larger trainable scope and capability-relevant objective outperform the rank-4 parity-MSE lane without authority regression."
    - id: claim-h11-htp-role
      statement: "HTP is classified by evidence as frozen-forward, teacher, updateable-section, zero-order, or blocked; it is not called normal backprop unless proven."
    - id: claim-h11-combined-povc
      statement: "The selected daemon/perf/scope/objective/backend choices run as one phone-native POVC with replayable artifacts and no critical falsifier."
  forbidden_proxies:
    - id: fp-doc-progress
      proxy: "Treating this PRD, handoff, or plan as Phase 11 progress."
    - id: fp-throughput-only
      proxy: "Promoting throughput while G1/G3/G8 or checkpoint correctness regresses."
    - id: fp-host-loop
      proxy: "Keeping host per-iteration ADB/process orchestration while claiming a phone-resident run."
    - id: fp-htp-inference-as-training
      proxy: "Calling QNN/HTP inference or platform validation Hexagon training."
    - id: fp-bigger-adapter-only
      proxy: "Counting trainable parameter increase as capability movement without loss/control/metric evidence."
---

<objective>
Execute Phase 11 as a sequential experiment campaign, H11-A through H11-H. The
campaign builds a phone-resident queue runner, measures the real bottleneck,
tests safe performance controls and OpenCL recordable queues, sweeps meaningful
trainable scopes, replaces parity-MSE with a capability-relevant objective,
classifies QAIRT/HTP mutable-section use, and combines the winning choices into
one phone-native POVC run.
</objective>

<context>
@docs/PRD-HARDWARE-NATIVE-TRAINING-POVC.md
@docs/HANDOFF-HARDWARE-NATIVE-TRAINING-POVC-EXECUTOR.md
@docs/research-packs/gemma4-heterogeneous-training-frontier-2026-05-18/08-OPUS-LEVEL-VIEW-AND-POVC.md
@.gpd/phases/10-hardware-max-training-pipeline/10-02-SUMMARY.md
@runtime/reports/gemma4_megakernel/hardware_max/20260517T153500Z_phase10_six_hour_endurance/gate_result.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: H11-A phone-resident daemon and queue</name>
  <action>Implement a long-lived phone runner with local queue, heartbeat, STOP file, runner_state.json, per-gate run dirs, resume behavior, and checksum_chain.jsonl. Preserve the one-shot path as fallback.</action>
  <verify>Run a bounded 50-iteration narrow-lane daemon test with checkpoint chain, disconnect test, active/wall comparison against Phase 10, and G1/G3/relevant G8 samples.</verify>
</task>

<task type="auto">
  <name>Task 2: H11-B safe performance envelope</name>
  <action>Probe and apply reversible no-root performance controls, fan/charge-separation observations, frequency/cooling telemetry, and stop thresholds.</action>
  <verify>Accept only a profile that is reversible, stays below stop thresholds, and improves measured daemon execution without authority regression.</verify>
</task>

<task type="auto">
  <name>Task 3: H11-C bottleneck autopsy</name>
  <action>Instrument compute, launch, tokenization, checkpoint, validation, storage, network, host pull, cold start, and idle waits in one-shot and daemon modes.</action>
  <verify>Account for at least 90 percent of wall time or residual under 5 s/iteration and explain the Phase 10 36.7 s/iteration dead time.</verify>
</task>

<task type="auto">
  <name>Task 4: H11-D OpenCL recordable queue probe</name>
  <action>Probe cl_qcom_recordable_queues support, function pointers, and no-op/mutable-arg dispatch timings; integrate only if correctness and end-to-end relevance are proven.</action>
  <verify>Record exact support or unsupported driver result; if enabled, outputs must match and timing must improve a measured bottleneck.</verify>
</task>

<task type="auto">
  <name>Task 5: H11-E trainable scope sweep</name>
  <action>Compare rank-4 post-layer0 baseline against larger meaningful scopes, including DoRA or LoRA r=16+ on q_proj, o_proj, gate_proj, and up_proj in later blocks where feasible.</action>
  <verify>Choose scope only by memory, active time, gradient correctness, checkpoint size, loss/capability signal, and non-regression evidence.</verify>
</task>

<task type="auto">
  <name>Task 6: H11-F objective upgrade</name>
  <action>Replace parity-MSE with a predeclared capability-relevant objective, first candidate top-k logit-KL distillation against frozen Gemma/HTP teacher or precomputed teacher shard.</action>
  <verify>Run fixed-control comparison; require train KL decrease, held-out signal non-regression, and declared mini capability metric improvement beyond fixed-adapter control.</verify>
</task>

<task type="auto">
  <name>Task 7: H11-G HTP mutable-adapter and zero-order arm</name>
  <action>Verify QAIRT host compile path, updateable tensor declaration, adapter binary updater, phone QnnContext_applyBinarySection path, and a bounded SPSA/MeZO-style forward-only update if the apply path works.</action>
  <verify>Classify HTP from evidence as frozen-forward, teacher, updateable-section, zero-order, or blocked; do not promote normal HTP backprop without API and parity proof.</verify>
</task>

<task type="auto">
  <name>Task 8: H11-H combined POVC run and falsifier report</name>
  <action>Combine the winning daemon, performance profile, recordable queue choice, trainable scope, objective, and optional HTP role into one queue-driven phone run.</action>
  <verify>Predeclared objective completes, artifacts replay, G1/G3/relevant G8 regressions pass, active/wall target or measured blocker is reported, and falsifier_report.md has no unresolved critical issue.</verify>
</task>

</tasks>

<success_criteria>
- H11-A through H11-H each produce gate_result.json, artifact_manifest.json,
  blockers.md, falsifier_report.md, and checksum records.
- The campaign does not execute host-driven per-iteration training after H11-A
  except as a documented fallback blocker.
- Any promoted claim preserves G1/G3 and relevant G8 authority metrics.
- The combined POVC produces replayable checkpoint/adapter manifests and a
  falsifier report.
- Failed hypotheses remain failed; no mixed evidence is converted into a pass.
</success_criteria>

<output>
After execution, write .gpd/phases/11-hardware-native-training-povc/11-01-SUMMARY.md
with exact pass/fail/blocker status per H11 gate and pointers to the phone,
RunPod, and git-allowed artifacts.
</output>
