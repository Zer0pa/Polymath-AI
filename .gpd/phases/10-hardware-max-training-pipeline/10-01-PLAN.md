---
phase: 10-hardware-max-training-pipeline
plan: 01
type: execute
wave: 1
depends_on: [09-01]
files_modified:
  - integrations/gemma4-snapdragon-megakernel/gemma4_megakernel/src/backends/opencl_layer_runner.cpp
  - runtime/reports/gemma4_megakernel/hardware_max/20260517T*_phase10_hf_auth_token_bridge_baseline/
  - runtime/reports/gemma4_megakernel/hardware_max/20260517T*_phase10_projected_ple_cache/
interactive: false

conventions:
  units: "seconds, KiB, FP32 tensors, FP64 comparison"
  metric: "actual REDMAGIC phone training run with non-regressing parity gates"
  coordinates: "batch-major sequence tensors"

contract:
  scope:
    question: "Can the first hardware-guided Phase 10 candidate improve the measured phone training path without weakening authority evidence?"
  claims:
    - id: "claim-phase10-projected-ple-cache"
      statement: "Caching projected PLE rows by token ID improves the phone token-to-hidden bridge for the current rank-4 two-layer training path while preserving all parity gates."
      deliverables: ["deliv-phase10-baseline", "deliv-phase10-optimized-run"]
      acceptance_tests: ["test-phase10-phone-ab"]
      references: ["ref-g10-final-falsifier", "ref-phase10-research"]
  deliverables:
    - id: "deliv-phase10-baseline"
      kind: "json"
      path: "runtime/reports/gemma4_megakernel/hardware_max/20260517T083219Z_phase10_hf_auth_token_bridge_baseline/gate_result.json"
      description: "HF-authenticated phone training baseline with token bridge split timing."
    - id: "deliv-phase10-optimized-run"
      kind: "json"
      path: "runtime/reports/gemma4_megakernel/hardware_max/20260517T084203Z_phase10_projected_ple_cache/gate_result.json"
      description: "Phone A/B run after projected PLE cache implementation."
  acceptance_tests:
    - id: "test-phase10-phone-ab"
      subject: "claim-phase10-projected-ple-cache"
      kind: "phone_training_ab"
      procedure: "Run the same HF-authenticated phone token cache and checkpoint through the baseline and optimized phone runner, compare every tensor against the RunPod PyTorch oracle, then require token-to-hidden elapsed time to improve."
      pass_condition: "Token cache, token-to-hidden, layer0, layer1, and adapter update reports are status pass; optimized token-to-hidden elapsed seconds are below baseline."
      evidence_required: ["deliv-phase10-baseline", "deliv-phase10-optimized-run"]
  forbidden_proxies:
    - id: "fp-microbenchmark-only"
      subject: "claim-phase10-projected-ple-cache"
      proxy: "Timing a helper function without a complete phone training run."
      reason: "The governing objective is training-path improvement under parity, not isolated timing."
    - id: "fp-throughput-over-correctness"
      subject: "claim-phase10-projected-ple-cache"
      proxy: "Promote a faster run with failed tensor parity."
      reason: "Authority metrics are sovereign."
---

<objective>
Instrument the Phase 10 baseline, select the first bottleneck from phone
telemetry, implement only the directly justified optimization, and accept it
only through an actual phone training A/B run.
</objective>

<context>
@.gpd/phases/10-hardware-max-training-pipeline/10-RESEARCH.md
@.gpd/phases/09-falsifier-review/09-01-SUMMARY.md
@runtime/reports/gemma4_megakernel/hardware_max/20260517T083219Z_phase10_hf_auth_token_bridge_baseline/gate_result.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add token bridge split telemetry</name>
  <files>integrations/gemma4-snapdragon-megakernel/gemma4_megakernel/src/backends/opencl_layer_runner.cpp</files>
  <action>Add timing fields for cache read, asset open, embedding gather, projection load, PLE layer0, PLE layer1, unique token count, cache rows, and active tokens.</action>
  <verify>Local CMake/CTest passes and baseline telemetry includes token_to_hidden_timing.</verify>
  <done>Baseline report exists and passes.</done>
</task>

<task type="auto">
  <name>Task 2: Run HF-authenticated phone baseline</name>
  <files>runtime/reports/gemma4_megakernel/hardware_max/20260517T083219Z_phase10_hf_auth_token_bridge_baseline/</files>
  <action>Use the stored Hugging Face token without printing it, fetch corpus on phone, tokenize/pack on phone, run --run-g8-distill, compare against RunPod PyTorch reference, and record a sanitized command log.</action>
  <verify>All comparator JSON files and gate_result.json are status pass.</verify>
  <done>Baseline bottleneck is ranked by actual phone telemetry.</done>
</task>

<task type="auto">
  <name>Task 3: Implement and test projected PLE cache</name>
  <files>integrations/gemma4-snapdragon-megakernel/gemma4_megakernel/src/backends/opencl_layer_runner.cpp</files>
  <action>Cache the full projected PLE row per token ID so repeated tokens avoid duplicate 256x2560 projection work.</action>
  <verify>Local CMake/CTest passes, Android build passes, and optimized phone training run passes all parity reports.</verify>
  <done>Optimized report exists and gate_result.json promotes only this narrow candidate.</done>
</task>

</tasks>

<success_criteria>
- Baseline and optimized runs are actual REDMAGIC phone `--run-g8-distill`
  executions.
- HF token is not printed, committed, or copied into artifacts.
- All tensor parity gates pass.
- Optimized token-to-hidden elapsed seconds are lower than baseline.
- The summary states non-claims and deferred work.
</success_criteria>

<output>
After completion, create
`.gpd/phases/10-hardware-max-training-pipeline/10-01-SUMMARY.md`.
</output>
