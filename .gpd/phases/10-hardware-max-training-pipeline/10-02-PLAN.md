---
phase: 10-hardware-max-training-pipeline
plan: 02
type: execute
wave: 1
depends_on: [10-01]
files_modified:
  - scripts/host/run_gemma4_phone_endurance.py
  - runtime/reports/gemma4_megakernel/hardware_max/20260517T*_phase10_six_hour_endurance/
  - runtime/reports/gemma4_megakernel/hardware_max/20260517T*_phase10_qnn_htp_probe/
  - runtime/reports/gemma4_megakernel/hardware_max/20260517T*_phase10_nonclaim_gate/
interactive: false

conventions:
  units: "seconds, FP32 tensors, SHA-256 checkpoint chain, Android thermal status"
  metric: "actual REDMAGIC phone training run; no report-only pass"
  coordinates: "batch-major sequence tensors, rank-4 adapter checkpoint chain"

contract:
  scope:
    question: "Which Phase 10 non-claims can be promoted by actual evidence, and which remain blocked by falsifiers?"
  claims:
    - id: "claim-phase10-six-hour-endurance"
      statement: "The current REDMAGIC rank-4 two-layer phone-native training path can sustain at least six wall-clock hours of repeated real training iterations while chaining checkpoints without corruption."
      deliverables: ["deliv-phase10-six-hour-report"]
      acceptance_tests: ["test-phase10-six-hour-chain"]
      references: ["ref-phase10-projected-ple-cache"]
    - id: "claim-phase10-hexagon-training"
      statement: "Hexagon/HTP can execute a real Gemma4 training update, not merely an inference or platform-validation workload."
      deliverables: ["deliv-phase10-qnn-htp-probe"]
      acceptance_tests: ["test-phase10-hexagon-training"]
      references: ["ref-qairt-local", "ref-device-qnn-libs"]
    - id: "claim-phase10-full-gemma4-training"
      statement: "The pipeline performs full Gemma4 training rather than the current two-layer rank-4 adapter distillation lane."
      deliverables: ["deliv-phase10-full-training-preflight"]
      acceptance_tests: ["test-phase10-full-training-scope"]
      references: ["ref-phase10-research"]
    - id: "claim-phase10-public-benchmark-ready"
      statement: "The run is ready for public benchmark submission with reproducible rules, corpus/license disclosure, benchmark harness, and all claimed hardware paths proven."
      deliverables: ["deliv-phase10-public-benchmark-preflight"]
      acceptance_tests: ["test-phase10-public-benchmark-readiness"]
      references: ["ref-public-benchmark-policy"]
    - id: "claim-phase10-theoretical-maximum"
      statement: "The Snapdragon/Gemma4 training lane has reached the theoretical maximum of the hardware for this task."
      deliverables: ["deliv-phase10-ceiling-model"]
      acceptance_tests: ["test-phase10-theoretical-maximum"]
      references: ["ref-phase10-research"]
  deliverables:
    - id: "deliv-phase10-six-hour-report"
      kind: "json"
      path: "runtime/reports/gemma4_megakernel/hardware_max/20260517T*_phase10_six_hour_endurance/gate_result.json"
      description: "Six-hour wall-clock phone training chain with telemetry, checkpoint hash chain, sampled raw tensors outside git, and sanitized commands."
    - id: "deliv-phase10-qnn-htp-probe"
      kind: "json"
      path: "runtime/reports/gemma4_megakernel/hardware_max/20260517T*_phase10_qnn_htp_probe/gate_result.json"
      description: "QNN/HTP platform and workload probe, explicitly separating inference/platform success from training-update support."
    - id: "deliv-phase10-full-training-preflight"
      kind: "json"
      path: "runtime/reports/gemma4_megakernel/hardware_max/20260517T*_phase10_nonclaim_gate/full_gemma4_training_preflight.json"
      description: "Evidence-backed preflight for whether current code covers all Gemma4 layers/parameters/training state."
    - id: "deliv-phase10-public-benchmark-preflight"
      kind: "json"
      path: "runtime/reports/gemma4_megakernel/hardware_max/20260517T*_phase10_nonclaim_gate/public_benchmark_readiness.json"
      description: "Pass/fail readiness gate for public benchmark disclosure, reproducibility, corpus license, and claim completeness."
    - id: "deliv-phase10-ceiling-model"
      kind: "json"
      path: "runtime/reports/gemma4_megakernel/hardware_max/20260517T*_phase10_nonclaim_gate/theoretical_maximum_gap.json"
      description: "Ceiling-gap falsifier that blocks theoretical-maximum claims unless the measured path is bounded against hardware counters and all candidate bottlenecks are exhausted."
  acceptance_tests:
    - id: "test-phase10-six-hour-chain"
      subject: "claim-phase10-six-hour-endurance"
      kind: "phone_training_endurance"
      procedure: "Run the projected-cache phone runner for at least 21600 wall-clock seconds; chain every output checkpoint into the next iteration; retain first/periodic/final raw samples outside git; verify all checkpoint manifests connect by SHA-256 and no iteration reports hidden fixtures."
      pass_condition: "Wall-clock seconds >= 21600, every iteration exits zero, checkpoint pre/post hashes form an unbroken chain and change on update, max Android thermal status remains below critical, and sampled parity checks pass against the RunPod PyTorch oracle."
      evidence_required: ["deliv-phase10-six-hour-report"]
    - id: "test-phase10-hexagon-training"
      subject: "claim-phase10-hexagon-training"
      kind: "qnn_htp_training_probe"
      procedure: "Deploy/run QAIRT/QNN validation and, if possible, a minimal HTP workload on the phone. Then inspect available QNN/QAIRT surfaces for a backward/update-capable training path."
      pass_condition: "A Gemma4 training update executes on HTP/Hexagon with no CPU/GPU fallback and has parity evidence. Platform validation or inference-only success is not sufficient."
      evidence_required: ["deliv-phase10-qnn-htp-probe"]
    - id: "test-phase10-full-training-scope"
      subject: "claim-phase10-full-gemma4-training"
      kind: "scope_falsifier"
      procedure: "Compare implemented trainable scope, layer coverage, optimizer state, and checkpoint schema against the full Gemma4 model graph."
      pass_condition: "All Gemma4 trainable parameters required by the declared training objective are updated or intentionally frozen under a documented full-training method, with corresponding checkpoint and parity gates."
      evidence_required: ["deliv-phase10-full-training-preflight"]
    - id: "test-phase10-public-benchmark-readiness"
      subject: "claim-phase10-public-benchmark-ready"
      kind: "benchmark_preflight"
      procedure: "Check reproducibility, public corpus/license disclosure, benchmark rule compliance, published command harness, hardware path proof, and non-regression evidence."
      pass_condition: "Every required benchmark field is present and every promoted claim has a passing authority gate."
      evidence_required: ["deliv-phase10-public-benchmark-preflight"]
    - id: "test-phase10-theoretical-maximum"
      subject: "claim-phase10-theoretical-maximum"
      kind: "ceiling_gap_falsifier"
      procedure: "Estimate the hardware roofline/IO ceiling for the measured training path and compare against observed throughput while listing untested optimization candidates."
      pass_condition: "Measured throughput is within the declared tolerance of the modeled ceiling and no untested candidate remains that could materially change the bottleneck."
      evidence_required: ["deliv-phase10-ceiling-model"]
  forbidden_proxies:
    - id: "fp-report-only-resolution"
      subject: "all"
      proxy: "Resolve a non-claim by writing prose that says it is now resolved."
      reason: "The governing objective is actual evidence, not narratable closure."
    - id: "fp-platform-validator-as-training"
      subject: "claim-phase10-hexagon-training"
      proxy: "Treat QNN platform validation or inference-only execution as Hexagon training."
      reason: "The non-claim is specifically training/backward/update on Hexagon."
    - id: "fp-short-run-as-endurance"
      subject: "claim-phase10-six-hour-endurance"
      proxy: "Treat a smoke test or three-batch chain as a six-hour endurance proof."
      reason: "The gate requires at least 21600 wall-clock seconds."
    - id: "fp-two-layer-as-full-model"
      subject: "claim-phase10-full-gemma4-training"
      proxy: "Treat the current rank-4 two-layer distillation path as full Gemma4 training."
      reason: "The current trainable scope is narrower by design."
---

<objective>
Attempt to liquidate the Phase 10 non-claims through hard gates. Promote only
the non-claims that pass actual evidence; record the others as blockers without
converting them into success language.
</objective>

<context>
@.gpd/phases/10-hardware-max-training-pipeline/10-RESEARCH.md
@.gpd/phases/10-hardware-max-training-pipeline/10-01-SUMMARY.md
@runtime/reports/gemma4_megakernel/hardware_max/20260517T084203Z_phase10_projected_ple_cache/gate_result.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add non-reward-hacking endurance harness</name>
  <files>scripts/host/run_gemma4_phone_endurance.py</files>
  <action>Create a host-side ADB harness that runs the existing phone training path for at least 21600 wall-clock seconds, chains checkpoints, stores small JSON audit artifacts in-repo, and keeps raw tensor samples outside git.</action>
  <verify>Run a short smoke with --allow-short-smoke only outside the repo report tree; default configuration must refuse to promote a sub-six-hour run.</verify>
</task>

<task type="auto">
  <name>Task 2: Execute six-hour phone endurance gate</name>
  <files>runtime/reports/gemma4_megakernel/hardware_max/20260517T*_phase10_six_hour_endurance/</files>
  <action>Run the projected-cache phone runner against the existing HF-derived token caches, cycling caches while chaining checkpoints until wall-clock duration exceeds six hours.</action>
  <verify>gate_result.json status pass, checkpoint chain pass, no hidden fixtures, no critical thermal state, no selected_text or token leaks in committed artifacts, sampled PyTorch parity pass.</verify>
</task>

<task type="auto">
  <name>Task 3: Execute Hexagon/QNN training falsifier</name>
  <files>runtime/reports/gemma4_megakernel/hardware_max/20260517T*_phase10_qnn_htp_probe/</files>
  <action>Run QNN/QAIRT platform/workload probes on the actual phone, then test whether any available path can execute the training update on HTP without fallback.</action>
  <verify>Only promote Hexagon training if a training update executes on HTP with parity; otherwise record a blocker with command evidence.</verify>
</task>

<task type="auto">
  <name>Task 4: Run scope and readiness falsifiers for the remaining non-claims</name>
  <files>runtime/reports/gemma4_megakernel/hardware_max/20260517T*_phase10_nonclaim_gate/</files>
  <action>Emit machine-readable gates for full Gemma4 training, public benchmark readiness, and theoretical-maximum status from implemented code coverage and current evidence.</action>
  <verify>Any fail remains a fail; do not create a summary that treats a failed gate as resolved.</verify>
</task>

</tasks>

<success_criteria>
- The six-hour claim is promoted only after >= 21600 wall-clock seconds and
  chain/hash/parity gates pass.
- The Hexagon claim is promoted only by an HTP training update, not QNN
  inference or validator output.
- Full Gemma4, public benchmark, and theoretical-maximum claims remain blocked
  unless their explicit gates pass.
- No Hugging Face token, selected text, raw tensor binaries, or model weights
  are committed.
</success_criteria>

<output>
After completion, create
`.gpd/phases/10-hardware-max-training-pipeline/10-02-SUMMARY.md` only with the
actual pass/fail statuses from the gates above.
</output>
