---
phase: "00"
plan: "00-01"
title: "Repair formal GPD control plane"
status: "executed"
objective: "Create formal GPD project state from the authority PRD without weakening the terminal phone-training gate."
---

# Plan 00-01: Repair Formal GPD Control Plane

## Tasks

- Read authority PRD, Resistance V2, fallback hypotheses/runlog, import
  manifest, G1 result, and upstream G1 handover/report.
- Probe local git, RunPod SSH, and REDMAGIC ADB.
- Create pivot branch `gemma4-megakernel-native-training`.
- Validate and persist a GPD project contract in `.gpd/state.json`.
- Add formal `.gpd/PROJECT.md`, `.gpd/config.json`, `.gpd/REQUIREMENTS.md`,
  `.gpd/ROADMAP.md`, `.gpd/STATE.md`, decisions, conventions, and phase
  directories.

## Verification

- GPD validator must accept the machine-readable project contract.
- `.gpd/` must expose terminal gate, G1 regression floor, artifact policy, and
  forbidden proxy wins.
- This plan does not claim terminal progress; it only repairs control-plane
  executability.
