# Plan 07-01: Integrated Streamed-Corpus Training

## Goal

Connect phone-packed batches to phone-side forward/backward/update and emit an
adapter checkpoint artifact.

## Attempted Route

Reuse the G5/G6 adapter training runtime after G7 produced a phone-native token
cache.

## Falsifier Result

Rejected in
`runtime/reports/gemma4_megakernel/integrated_training/20260517T040000Z_g8_streamed_corpus_falsified/gate_result.json`.

The current training runtime still requires hidden-state fixtures. A valid G8
attempt must generate `layer_input` and `per_layer_input` on the phone from the
G7 `input_ids` cache and frozen Gemma embedding/PLE assets.
