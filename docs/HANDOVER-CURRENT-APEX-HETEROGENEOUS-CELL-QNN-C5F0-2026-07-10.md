# Current Apex Heterogeneous Cell Operational Handover Prompt

Copy the text below into the successor agent's task.

---

You are assuming operational custody of the current Apex Heterogeneous Cell
engineering state in:

`/Users/Zer0pa/Polymat AI/Polymath-AI`

This is a current-reality-only handover. Do not infer, search for, or ask the
operator to restate historical context. Do not follow the living PRD's links to
older report sequences, older handovers, broad pipeline updates, or historical
design briefs. The user will separately supply any historical artifacts they
want admitted.

## First Turn: Context Ingestion And Report For Duty Only

Do not connect to the phone or RunPod. Do not run the frozen C5F0 command,
native hardware tests, QNN or Gemma execution, experiments, science gates,
projection work, activation capture, gradient work, D1/D2/D3/H5, Gate D2A,
Gate E, Comet logging, commits, pushes, or external mutations.

Read these current artifacts in order:

1. Living current-state PRD:
   `docs/PRD-PHASE34-TASK-ALIGNED-PROJECTION-PHONE-NATIVE-GEMMA4-E4B-POLAR-TRAINING-2026-07-08.md`

2. FIR — Frontier Innovation Register / current hardware-native engineering
   register:
   `docs/APEX-HARDWARE-NATIVE-QNN-GEMMA4-SOC-ENGINEERING-IDEAS-AND-IMPLEMENTATION-OPTIONS-2026-07-10.md`

3. Current RunPod/QAIRT/QNN operational runbook. Use only the 2026-07-10
   override; the lower broad-inventory/rsync section is legacy and not
   execution authority:
   `docs/runbooks/RUNPOD-QUALCOMM-GEMMA4-MEGAKERNEL-SETUP.md`

4. Q1N authority evidence:
   `runtime/reports/apex_heterogeneous_cell/q1n_nonzero_20260710T190640Z/q1n_phone_native_nonzero_parity_report.json`
   SHA-256:
   `803d8a952ee9b4db970772626e82ea64ad5b94ae26fb17f3979cda6ae0ff003f`

5. Q1N final disposition:
   `runtime/reports/apex_heterogeneous_cell/q1n_nonzero_20260710T190640Z/q1n_final_disposition_report.json`
   SHA-256:
   `e701ce1a06b561021f2d6fffb67e0e0a40d6da68338ce048b3e08a2928d69830`

6. Frozen C5F0 attempt-12 readiness packet:
   `runtime/reports/apex_heterogeneous_cell/c5f0_live_preflight_readiness_20260710T192335Z/c5f0_attempt12_readiness_report.json`
   SHA-256:
   `1e51122a1fed4fab0e89a9b0136e132fa146e59fd38bf9429489379182859dad`

Within the PRD, Section 23 is the controlling operational capsule and Section
22.20 is the current execution/evidence order. Earlier provider examples and
agent-launch packets are superseded wherever they conflict.

## Sovereign Current State

- Gate E remains falsified.
- Promotion is forbidden.
- Sovereign full27 result:
  - `before_nll_per_token: 14.538868526231177`
  - `after_nll_per_token: 14.555113501137114`
  - `nll_delta_per_token: +0.01624497490593768`
- After-minus-before NLL must be negative to pass. A positive delta is failure
  regardless of rank, correlation, polar loss, throughput, or hardware wins.
- Scientific blockers remain:
  - `layer24_plus_two_alternatives_not_fully_measured`
  - `phase34c_gradient_rank_probe_instrument_absent`
  - `no_rank_matched_projection_candidate`
- Never synthesize `dL_NLL/dh` from activation, correlation, H3/H4, or
  historical finite-difference surfaces.

## Current Engineering Closure

Q1N passed on the RedMagic phone:

- two distinct nonzero inputs and two distinct nonzero wrapper controls;
- 2/2 required HTP wrapper executions with no QNN graph CPU fallback;
- four exact pidfd child-lifecycle receipts;
- 32 direct `graphExecute` calls through one persistent context/graph;
- 16 A parity checks plus 16 A wrong-control rejections;
- 16 B parity checks plus 16 B wrong-control rejections;
- 31 A/B transitions and 32 poison overwrites;
- 32/32 current-call HTP profile snapshots;
- balanced teardown and zero surviving relevant processes; and
- metadata-only Comet receipt:
  `https://www.comet.com/zer0pa-imc/mobile-polymath-ai-training/989edd7f986949b796a2709dc3a1b519`.

Interpret this narrowly. Q1N closes nonzero discriminatory one-layer
wrapper/direct persistent-HTP parity. It does not close C5F0, decoder
architecture fidelity, dimensionality, gradient instrumentation, candidate
construction, Gate D, or Gate E.

## Frozen Next Boundary

C5F0 attempt 12 is completely staged but deliberately unexecuted:

- first missing green field to test:
  `explicit_system_linker_plus_payload_maps_identity_not_revalidated`
- phone staging root:
  `/data/data/com.termux/files/home/polymath_c5f0_preflight/20260710T192335Z`
- exactly two staged source files match their frozen hashes;
- report output is absent;
- relevant process count at final readiness check was zero;
- `execution_authorized: false`;
- `execution_launched: false`;
- frozen command SHA-256:
  `63c9e3e3731f8fcb2dba4b00f446357bd47beaf052425b3cba5ea910570e2c85`;
- local closure: 76 tests passed, one expected host-only pidfd skip;
  compilation, lint, JSON validation, secret scan, and diff checks were green.

The next executable action, only after fresh explicit user authorization, is
the already frozen synthetic C5F0 attempt-12 phone preflight. It must not be
silently widened. It runs custody mechanics only—no Gemma, QNN science,
corpus, activation, gradient, projection, or gate.

## Platform Custody

Phone:

- RedMagic / NX789J / SM8750 / Android 15;
- ADB serial `FY25013101C8`;
- Termux user `u0_a536` and home `/data/data/com.termux/files/home`;
- primary practical compute, raw storage, QNN/HTP, and Vulkan/Adreno surface;
- ADB forwards Mac TCP 18022 to device TCP 8022;
- current secure SSH procedure, recovery steps, known-hosts file, and phone
  roots are frozen in PRD Section 23;
- an SSH banner timeout is not evidence that a phone run failed; refresh the
  ADB forward, wake Termux, then inspect sidecars and process state.

Mac:

- control plane, source tree, and metadata ledger only;
- do not recreate or pull raw corpus rows, activation/gradient rows, tokens,
  logits, tensors, profiles, prediction JSONLs, theta, model payloads, QNN
  contexts/model libraries, SDK binaries, or raw logs under
  `/Users/Zer0pa/Polymat AI`.

RunPod:

- pod id `uh57jg7iguwqth`;
- private SSH alias `runpod-polymath-qnn-build-20260710`;
- endpoint, port, signed terminal URL, and private key remain outside the repo;
- pinned host keys and `StrictHostKeyChecking=yes` are mandatory;
- CPU-only QAIRT/QNN foundry; no GPU is present, used, or required for its
  current role;
- exact QAIRT root `/workspace/qairt-2.44/qairt/2.44.0.260225`;
- SDK `v2.44.0.260225143659`, target `socModel 69`, HTP V79;
- host Ubuntu 20.04/glibc 2.31 cannot run the current QAIRT tools directly;
  use the retained isolated Ubuntu 22.04/glibc 2.35/Clang 14 runtime;
- new work uses `/workspace/polymath_qnn_build_plane/<UTC_RUN_ID>`, never the
  inherited `/workspace/Polymath-AI` clone;
- provider-local Q0B context SHA-256
  `9060753282c8b0bf232b7db8abc1637b6d9f857560c2d151ae8cd3f197e6aa03`
  proves one synthetic `[1,16,2560]` float32 ReLU context only;
- do not upload phone raw data, model weights, corpus material, or authority
  payloads to RunPod;
- do not provision a pod GPU unless a later, separately governed workload and
  explicit user authorization require it.

## Hardware-Native Direction

The FIR records the current engineering architecture and hypotheses:

- capability-sealed QAIRT adapter;
- mmap/lazy context loading;
- scoped HTP performance-policy controller;
- registered DMA-BUF I/O rings;
- one persistent heterogeneous phone cell;
- persistent Vulkan resources;
- explicit QNN/Adreno shared-allocation and fence ownership;
- asynchronous QNN depth two;
- fixed-shape context atlas and dependency-frontier batching;
- multi-graph packaging followed by separately proven static fusion;
- mutable-theta QNN seam;
- native PJP1 staging;
- VTCM/spill accounting;
- direct-mode reference arm;
- unified structural-complexity ledger; and
- hardware-assisted scientific instruments.

These are implementation options, not performance or authority claims. Do not
claim zero-copy, pinned clocks, graph fusion, VTCM residency, eliminated
runtime decisions, or mutable training support merely because an API, tool, or
symbol exists.

## Git And GitHub Hygiene

- Remote: `https://github.com/Zer0pa/Polymath-AI.git`
- Branch: `gemma4-megakernel-native-training`
- Observed local HEAD at handover:
  `d7a89e1c4a495436ac6bc535bc115f4d87f5caff`
- The worktree has broad inherited dirty/untracked drift.
- Preserve unrelated work. Use scoped status/diff only.
- Never run global clean, `git reset --hard`, blanket checkout/restore,
  `git add -A`, or a worktree-wide formatter.
- Do not stage, commit, push, branch, or open a PR without the applicable
  explicit user authorization.
- GitHub is source custody and review, not scientific authority.

## Comet Hygiene

- Workspace: `zer0pa-imc`
- Project: `mobile-polymath-ai-training`
- Metadata-only reports/assets and small result JSON receipts.
- Source the phone environment without printing any value.
- Never run `env`, `set`, `printenv`, shell tracing, or commands that echo
  credential variables.
- Use only:

  `python3 scripts/host/log_apex_metadata_report_to_comet.py --gate PHASE34 --report <metadata-report.json> --run-name <descriptive-name> --output <new-result.json>`

- Verify report SHA binding, target workspace/project, asset status,
  experiment URL, and result SHA.
- Dashboard presence is operator visibility, not authority evidence.

## Raw And Secret Boundary

Never print, embed, transfer, commit, or upload secrets, environment values,
private endpoints, keys, raw rows, tokens, logits, tensors, raw profiles,
prediction JSONLs, theta, model weights, QNN contexts/model libraries, SDK
binaries, payload dumps, or unrestricted stdout/stderr. Phone-native raw
custody is mandatory. RunPod retains only its explicitly bounded foundry
scratch. Mac, GitHub, and Comet receive only source where authorized, small
schemas/reports, hashes, byte counts, shapes, aggregate metrics, sanitized
command ledgers, and small receipts.

## Report-For-Duty Response

After reading the six artifacts, respond briefly and confirm:

- Gate E remains falsified and promotion is forbidden;
- Q1N passed, with its closure stated narrowly;
- C5F0 attempt 12 is source-matched, output-absent, process-clean, staged, and
  unexecuted;
- the three remaining scientific blockers;
- phone-primary, Mac-metadata-only, CPU-only-RunPod-foundry custody;
- metadata-only Comet and scoped Git/GitHub discipline;
- the FIR is an engineering hypothesis register, not execution authority; and
- you are ready for explicit user direction and will not execute the frozen
  C5F0 command or any broader native hardware/science work before that
  authorization.

---
