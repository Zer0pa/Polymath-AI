# CPU Kernels

CPU reference kernels live in:

- `polymath_native/include/polymath_native/cpu_kernels.h`
- `polymath_native/src/cpu_kernels.cpp`

They are the correctness authority for early OpenCL and Vulkan backend work.
