# Phase 2-C Next Real Corpus Gate

Status: phase2c_real_corpus_required

Run the optimized native PQA1-to-PJP1 path on real training material at 100k and 1M scale. Required pass gates: Comet logged, PJP1 readback pass, stratified oracle pass, >=50k distinct-token throughput >=200000 real tok/sec, collision duplicate-token fraction <=0.1%, margin/Hamming geometry no worse than synthetic proxy baseline without explanation, and no Phase 3 handoff until NPU consumer preflight is repeated on real PJP1.
