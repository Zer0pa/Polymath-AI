# Blockers

None for the predeclared three-batch sustained authority objective.

Scope note: this is a chained multi-batch authority run, not a six-hour endurance run. A future long-duration run should reuse the same checkpoint-chain and falsifier harness with native thermal headroom polling.
